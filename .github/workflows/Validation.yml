name: Validation Test Report Workflow

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  validation-report-job:
    name: Process Validation Test Report
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify Validation Test Report Excel File
        run: |
          if [ -f "tests/reports/Validation_Test_Report.xlsx" ]; then
            echo "Successfully found tests/reports/Validation_Test_Report.xlsx"
          else
            echo "Error: tests/reports/Validation_Test_Report.xlsx does not exist!" && exit 1
          fi

      - name: Upload Validation Test Report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Validation-Test-Report
          path: tests/reports/Validation_Test_Report.xlsx
